package in.automationtesting.classandobject.datatypes

class GroovyDataTypesDemo {
    static main(args) {
        def byteVariable = 10
        println "byte value is: " + byteVariable

        def shortValue = 1000
        println("short value is:" + shortValue)

        def intVariable = 10000
        println("Integer value is:" + intVariable)

        def longVariable = 10000000L
        println("long value is: " + longVariable)

        def floatVariable = 10.2f
        println("float vairable is " + floatVariable)

        def doubleValue = 59.34
        println("double value is " + doubleValue)

        def booleanValue
        println("boolean value is " + booleanValue)

        def charValue = '$'
        println("charvalue is " + charValue)

    }
}
